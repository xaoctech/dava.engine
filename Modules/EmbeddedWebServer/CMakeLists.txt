set( NAME_MODULE EmbeddedWebServer )

set( MODULE_TYPE STATIC )
set( INCLUDES "Sources/" )                                     # set search path headers for all project

set( NOT_USE_PARENT_DEFINITIONS true )

setup_main_module()
