#include "Base/IModule.h"

@IMODULE_INCLUDES@

namespace DAVA
{
    Vector<IModule*> IModule::listModules;

    void IModule::InitModules()
    {
@IMODULE_CODE@
    }

    void IModule::ReleaseModules()
    {
        for (const auto& module : listModules)
        {
            module->Shutdown();
            delete module;
        }
    }    
}