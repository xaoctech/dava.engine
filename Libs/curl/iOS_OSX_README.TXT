//A simple way to build curl for iOS and OSX is run curl-ios-build-scripts-master/build_curl
// Run it from terminal
//https://github.com/brunodecarvalho/curl-ios-build-scripts.git
//it fetches curl from github (you can specify the version. Look at --help)
//and builds it for all architectures for OSX and iOS
// any build flags could be changed inside compiler.rb